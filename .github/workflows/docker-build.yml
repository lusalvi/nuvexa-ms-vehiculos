# Archivo: .github/workflows/build-and-push.yml (Vehiculos)
name: Build and Push Docker Image - Vehiculos

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Build and Push Vehiculos Service
        uses: lusalvi/nuvexa-frontend/build-push-microservice@main
        with:
          service-name: 'vehiculos'
          docker-registry: 'lusalvi'